let numero = parseInt(prompt ("Informe um número para receber o dobro."));
function verificarNumero(numero){
       let resultado = (numero*2)
       alert (`O resultado é ${resultado}`)
}
verificarNumero (numero)

