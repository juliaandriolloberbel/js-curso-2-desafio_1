let numero1 = parseInt(prompt("Digite um numero para aparecer o resultado da multiplicação por ele mesmo:"));
function verificarAlert(){
    let resultado = numero1*numero1
    alert (`O resultado é ${resultado}`);
}
